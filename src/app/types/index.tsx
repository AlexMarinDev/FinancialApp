export type TCustomer = {
  id: string;
  first_name: string;
  last_name: string;
  address: string;
  zip_code: string;
  city: string;
  country: string;
  country_code: string;
  company: string;
}

export type TMyCompanies = {
  id: string;
  name: string;
}